export default interface IHistoryEntry {
    tripId: string;
    userId: string;
    name: string;
    reservedAmount: number;
    purchaseDate: string;
    country: string,
    startDate: string,
    endDate: string,
    totalPrice: number,
  }