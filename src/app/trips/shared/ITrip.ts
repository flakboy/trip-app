export default interface ITrip {
    name: string;
    country: string;
    startDate: string;
    endDate: string;
    desc: string;
    price: number;
    thumbnail: string;
    available: number;
    id: string;
    images: string[];
}